<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="author">
        <h2 class="jh-entity-heading" data-cy="authorDetailsHeading">
          <span v-text="t$('bookApp.author.detail.title')"></span> {{ author.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="t$('bookApp.author.firstName')"></span>
          </dt>
          <dd>
            <span>{{ author.firstName }}</span>
          </dd>
          <dt>
            <span v-text="t$('bookApp.author.lastName')"></span>
          </dt>
          <dd>
            <span>{{ author.lastName }}</span>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="t$('entity.action.back')"></span>
        </button>
        <router-link v-if="author.id" :to="{ name: 'AuthorEdit', params: { authorId: author.id } }" custom v-slot="{ navigate }">
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="t$('entity.action.edit')"></span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./author-details.component.ts"></script>
